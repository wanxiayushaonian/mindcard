# MindCard 产品愿景（VISION）

> 这份文档是 MindCard 的**产品宪法**：它定义"我们为什么做这个产品"以及"什么是对的"。
> 技术实现细节见 `CLAUDE.md`；这里只讲理念、方向与决策准则。开发新功能前，先问它是否符合这份愿景。

---

## 一句话愿景

**MindCard 不是"AI 聊天 + 卡片"，而是一套完整的认知工作流：用对话分叉承载发散思维，用拓扑森林承载知识沉淀，用检索增强服务连接，用更强 LLM 完成收敛产出，用复现让知识持续复利。**

核心循环：

```
     发散              沉淀              收敛              复现
┌─────────┐    ┌──────────────┐    ┌────────────┐    ┌──────────┐
│ 对话分叉 │ →  │ 卡片挂载拓扑  │ →  │ 更强LLM    │ →  │ 定期重遇  │ ─┐
│ (探索)   │    │ 森林 (知识)   │    │ 总结/分析   │    │ (浮现)    │  │
└─────────┘    └──────────────┘    └────────────┘    └──────────┘  │
      ↑                                                     │
      └──────────────────── 重新进入下一轮探索 ←───────────────┘
```

**没有"复现"这一环，前三个环节只是单向流的档案柜；有了它，系统才成为活的认知体。**

---

## 十大理念

### 1. 卡片笔记的本质（Zettelkasten）

MindCard 首先是**卡片笔记软件**，必须满足卡片笔记的核心要素：
- **原子性**：每张卡片一个想法，过大卡片应被提示拆分（编辑器已有 `atomicityThreshold` 提示）
- **双向链接**：卡片之间通过 `CardRelation` / 编辑器 `WikiLink` 建立联系
- **卡片是知识的原子单元**：LLM 的沉淀物、检索的结果、收敛的输入，都以卡片为最小载体

### 2. 灵感的诞生地：从"工作现场"搬到"AI 老师面前"

传统卡片笔记的痛点是：灵感诞生于**其它工作**（阅读、写作、思考），然后要整理思路 → 手动记录 → 放入卡片库 → 补链接。

MindCard 把**灵感生成的地方**搬到 AI agent 面前——让 AI 作为"聪明的老师"，在对话中引出、催化、记录你的想法。**对话本身就是灵感现场**，沉淀是自然的，不是事后补录的。

### 3. 对话分叉 = 发散思维的结构化

卡片笔记的创作是**跳跃性 / 发散性**的思维。MindCard 的**智能对话分叉**（自动 fork + 手动 fork）正是支撑这种发散的结构化工具：
- 思维岔开了 → 分叉，各自独立探索
- 已实现的机制：`create_fork` LLM 工具、`split_guard` 防过度分叉、`fork_compress` 上下文压缩、前端折叠/动画/展开持久化

### 4. 双层拓扑：对话历史树 × 知识卡片树

这是 MindCard 的核心架构，也是最容易混淆的地方。存在**两层树**，职责完全不同：

| 层 | 是什么 | 职责 | 载体 |
|----|--------|------|------|
| **对话分叉树** | 对话/子对话的历史 | **agent 上下文工程**：每分支拥有独立 LLM 上下文与事件流 | `AiChat.parent_id` 树 |
| **知识拓扑树** | 从各对话沉淀的卡片 | **卡片笔记本身**：知识卡片挂载到其诞生节点 | `NodeCard` 绑定 |

**挂载准则（关键）**：
- 卡片从"哪个对话"沉淀，就挂到**哪个对话节点**下
- 判定依据是**当前正打开的对话**（`activeForkId || chatId`），而不是文本内容比对
- 一般打开的对话，就是用户要记录沉淀卡片的地方 → 这是**源码挂载**

### 5. 检索是插件，不是核心

**RAG（普通向量检索）和图谱（GraphRAG 推理检索）都是卡片信息获取的增强插件**，不是产品本体：
- 它们服务于"在发散对话中，把已有知识喂给上下文"
- 现有实现：`retrieval_dispatcher` 5 级检索（CHAT/SEARCH/EXPLORE/CONTEXT/INSIGHT）、RRF 混合检索、`gnn_retriever` 图遍历、`community` 社区报告

**推论**：检索系统可以替换、升级、插件化，但沉淀系统（卡片 + 拓扑）是本体，不能被检索逻辑绑架。

### 6. 森林级收敛：发散的思想 → 有产出的报告

用户在发散对话中不断探索并沉淀卡片，与老师扩展的思维与见解全被记载在**拓扑森林**中。森林本身就是一份"思维档案"，可以交给**能力更强的 LLM**：

- **产出目的**：把发散的思想总结成一篇报告（论文、文章、方案……）
- **元认知目的**：分析你的思维路径，纠正错误的思维习惯
- **粒度**：不只是单分支/单主题总结（现有 `consolidation` / `synthesis` 已做），而是**整片森林**的收敛

### 7. 复现循环：让知识复利（待建）

卡片写完就沉底 = 知识墓地。真正的学习系统要"重遇"：
- **机制**：基于记忆时间戳（`memory_decay.last_accessed_at` 已存在）定期浮现——"你三个月前在这条分支写过 X，和今天的问题相关，要不要重访？"
- **意义**：发散 → 沉淀 → **重遇** → 再发散，才是完整学习环。没有它，森林只是越堆越大的档案柜。

### 8. 桥接建议：该连而未连（待建）

卡片笔记的灵魂是链接，但人不会主动连。机制：
- 定期扫描森林中 **embedding 语义相近但无 `CardRelation` / `NodeRef`** 的卡片对
- 向用户建议"这两张该连"，由用户确认
- **这是用检索增强（理念5的插件思维）反向服务沉淀层（理念4）**

### 9. 思维画像：被笔记认识（待建）

理念6的深度化——让森林**照见使用者自己**：
- "你 80% 的发散都从技术出发，很少从人/体验出发"
- "你的 X 类想法总是沉不下来（创建后从不重访）"
- 每次收敛报告不只总结内容，还附一份**这次探索的思维模式摘要**
- 把用户从"用笔记"升级成"被笔记认识"

### 10. 苏格拉底式引导：老师不只是回答（待建）

理念2说"搬到聪明的老师面前"，但老师的职责不只是**答**，更是**引**：
- 在分叉点反问："这个想法和你'NLP'分支里的那张卡片有什么关系？"
- 发散过度时拉回："我们离主线越来越远了，要不要先收敛当前这条？"
- 现有 `challenge` fork profile 已埋种子，把它从"一种 profile"升级成"老师的基础姿态"

---

## 理念 ↔ 代码现状映射

| 理念 | 现状 | 状态 |
|------|------|------|
| 1 卡片笔记 | `Card`/`CardRelation`/WikiLink/原子性提示 | ✅ 已实现 |
| 2 灵感诞生地 | AI 聊天 + `consolidation` 沉淀记忆 + 划选沉淀卡片 | ✅ 基础已实现 |
| 3 发散分叉 | fork 全套（工具/守卫/压缩/折叠/持久化） | ✅ 已实现 |
| 4 双层拓扑 | `NodeCard` 绑定 + `assign_card_to_node` 源码挂载 | ✅ 已实现（源码即归属） |
| 5 检索插件 | 5 级检索 + RRF + 图遍历 + 社区报告 | ✅ 已实现 |
| 6 森林级收敛 | `consolidation`(单会话) / `synthesis`(主题级) / INSIGHT(社区级) | ⚠️ **缺森林级** |
| 7 复现循环 | `memory_decay.last_accessed_at` 已有种子 | ❌ 待建 |
| 8 桥接建议 | embedding 相似度扫描可作为基础 | ❌ 待建 |
| 9 思维画像 | 森林数据齐全，缺分析服务 | ❌ 待建 |
| 10 苏格拉底引导 | `challenge` profile 埋种 | ❌ 待建 |

---

## 关键偏差与修正优先级

### P0 — 源码挂载（理念4）✅ 已实现

**现状**：`topology_service.assign_card_to_node` 用 **embedding 相似度**决定卡片挂到哪个节点。
**愿景**：卡片应挂到**它诞生时所在（当前打开的）对话节点**。

**修正**（双信号）：
1. 前端在聊天面板沉淀卡片（precipitate）时，把**当前活跃对话**（`activeForkId || chatId`）作为 `chat_id` 传给后端
2. 后端 `assign_card_to_node` 增加 source 参数：有 source → 挂 source 节点；无 source（如卡片列表直接创建）→ 退回 embedding 分类

**为什么**：卡片的意义来自它**诞生的语境**，而非它字面上像什么。embedding 应服务"检索"（理念5），不该服务"归属"（理念4）。

### P0 — 森林级收敛（理念6）

**现状**：收敛只有单会话/单主题级。
**愿景**：把整片拓扑森林（全部分叉 + 沉淀卡片）交给更强 LLM，产出报告 / 思维分析。

**修正**：
1. 设计"森林快照"：把拓扑森林序列化为一份可注入的上下文（分支结构 + 各分支摘要 + 卡片清单）
2. 新增收敛服务：调用更强 LLM，输出（a）结构化的主题报告（b）思维路径分析
3. 前端：收敛结果页（现有 `synthesis` 页可作起点）

### P1 — 复现循环（理念7）

基于 `memory_decay` 的访问时间戳做浮现：定时扫描"久未重访但相关"的卡片/分支，在对话中提议重访。

### P1 — 桥接建议（理念8）

扫描语义相近但未连接的卡片对，生成"建议连接"列表，用户确认后写入 `CardRelation` / `NodeRef`。

### P2 — 思维画像（理念9）

积累森林的思维特征统计，在收敛报告中附"思维模式摘要"。

### P2 — 苏格拉底引导（理念10）

重构对话 prompt 层：加入"引导性反问"与"发散拉回"的姿态，与分叉机制联动。

---

## 开发决策准则

1. **一切功能服务"认知循环"**（发散→沉淀→收敛→复现）。与循环无关的功能不优先做。
2. **检索服务沉淀，不服务归属。** 检索（RAG/图谱）是插件，归属（卡片挂载）必须溯源。
3. **LLM 是老师，不是复读机。** 对话设计应引导用户产出想法，而非单纯回答问题。
4. **沉淀优先于美化。** UI/UX 优化的最高价值是"让 agent 的思考过程可见"（工具时间线、计划面板、卡操作确认），而非装饰性美化。
5. **复现是闭环的钥匙。** 任何让系统"只积累不生长"的设计都是负债。
6. **框架引入的前提**：缺的能力是"通用的、框架现成的、且与语言/抽象同构"。当前 agent 能力在 Python 增量扩展即可，无需引入 TS/Node 的编码 agent 框架（如 pi）。
